stages:
  - build
archive:
  stage: build
  script:
    - php7.0 /usr/bin/composer -d=includes install
    - shopcli build:archive "$CI_PROJECT_DIR" -n -v -o "$HOME/archives/shop-$CI_COMMIT_REF_SLUG.zip"
  only:
    - master
    - /^release\/.*$/
    - tags
  tags:
    - build
